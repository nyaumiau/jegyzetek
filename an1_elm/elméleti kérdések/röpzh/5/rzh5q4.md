#### 4. Igaz-e az, hogy ha az $(a_n)$ és a $(b_n)$ sorozatoknak van határértéke és $a_n \gt b_n$ minden n-re, akkor $\lim(an) \gt \lim(bn)$ ?
Legyen $(a_n) := n+1, (b_n) := n.$ Ekkor: $$\forall n \in \mathbb{N}:a_n \gt b_n, \text{ viszont: } \lim(a_n)=\lim(b_n)=+\infty$$ Ezért az állítás hamis.
